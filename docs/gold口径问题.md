# gold 口径问题（已定案：统一用 gold_v3）

> **2026-09-22 定案：基准统一为 `output/gold_v3.md`（章 8 / 节 43 / 段 113），
> 不再并用两套。** `output/gold.md`（6/30/87）与 `output/gold_v2.md`（8/28/83）已停用，
> 保留仅为记录。权威对照表见 `output/gold_v3_baseline.txt`。
>
> 下文记录当初为什么会有两套、以及它们的分歧有多大 —— 这是定案的依据，不是待决事项。

---

# 两套 gold 的口径分歧，以及它如何决定结论

2026-09-22 收到第二份 gold（`output/gold_v3_claude.json`，Claude 产出，
已转成 `output/gold_v3.md` 供 goldeval 读取）。用它重跑全部对比后，**节/段级的结论翻转**。

## 两套 gold 本身的差距

| 层级 | gold_v3 | gold_v2 | F1(0) | F1(±3) | WinDiff |
| --- | ---: | ---: | ---: | ---: | ---: |
| 章 | 8 | 8 | 0.857 | 0.857 | **0.028** |
| 节 | 43 | 28 | 0.551 | 0.667 | **0.244** |
| 段 | 113 | 83 | 0.577 | 0.773 | **0.257** |

**章级两套 gold 高度一致**（WinDiff 0.028），**节/段级分歧很大**（0.244 / 0.257）。

对照模型的成绩（段级 WinDiff 中位）：v8 0.222、v6 0.300。
也就是说 **v8 与 gold_v3 的吻合度（0.222），比 gold_v2 与 gold_v3 的吻合度（0.257）还高。**
两套人工/半人工参考之间的分歧，已经等于甚至超过模型与参考的分歧。

## 换 gold 之后的排名翻转

段级 WindowDiff 中位（v6 / v8 各 5 次）：

| 基准 | v6 | v8 | 谁赢 |
| --- | ---: | ---: | --- |
| gold_v2（83 段） | **0.300** | 0.315 | v6 |
| gold_v3（113 段） | 0.300 | **0.222** | **v8** |

节级同样翻转：gold_v2 下 v6 0.248 / v8 0.251，gold_v3 下 v6 0.281 / **v8 0.220**。

原因不神秘：**v8 的切分粒度（段数中位 107）本来就接近 gold_v3 的 113，而远离 gold_v2 的 83。**
分数主要反映「像哪一套 gold」，不反映「对不对」。

## 章级是例外，那一层的结论稳定

两套 gold 章级 WinDiff 只有 0.028，说明章边界有客观性。在这一层：

| 方案 | 块数中位 | WinDiff（gold_v2） | WinDiff（gold_v3） |
| --- | ---: | ---: | ---: |
| v6 | 8 | 0.057 | 0.057 |
| v8 | 12 | 0.257 | 0.284 |
| v7 | 11 | 0.306 | 0.279 |
| v9 | 14 | 0.324 | 0.351 |

**换哪套 gold 都一样：v6 的章级最好，v7/v8/v9 都明显更差。**
这条结论不受口径影响，可以采信 —— 我对章规则的改动（v7/v9）和段长收紧的连带效应（v8）都伤了章级。

## 结论

1. **章级可以用 gold 评判**，两套口径一致。当前最好的是 v6（保持 v6 的章规则不动是对的）。
2. **节/段级不能只用单一 gold 评判**。两套 gold 自己就差 0.244/0.257，
   在这个噪声上比较 0.02~0.08 的模型差异没有意义。
3. **要在节/段级得出可信结论，必须先定口径**：
   - 究竟要 83 段还是 113 段的粒度？这是产品决策（下游检索的 chunk 大小），不是模型问题。
   - 口径定下来之后，再按「每版 5 次取中位」比较。
4. v8 不是「变好了」，是**评判标准换成了与它粒度相近的那一套**。
   同理 v6 在 gold_v2 下的优势也有相当部分来自粒度巧合。

## 附：文件对应关系

| 文件 | 来源 | 规模 |
| --- | --- | --- |
| `output/gold.md` | 与 gold_v2 同批提交，另一套切分 | 6 / 30 / 87 |
| `output/gold_v2.md` | README 指定的基准 | 8 / 28 / 83 |
| `output/gold_v3_claude.json` | 2026-09-22 提供，Claude 产出 | 8 / 43 / 113 |
| `output/gold_v3.md` | 由上面的 json 无损转换而来 | 8 / 43 / 113 |

三套参考切分的段数是 87 / 83 / 113，彼此都不一致。

---

# 以 gold_v3 为基准，全部版本的排名（2026-09-22）

★ = 本次会话新做的版本（v7 / v8 / v9）

## 节（gold 43 块）—— 新版本包揽前三

| 方案 | 块数 | F1(0) | WinDiff |
| --- | ---: | ---: | ---: |
| ★ v8 | 36 | 0.609 | **0.220** (n=5, 0.185~0.264) |
| ★ v7 | 37 | 0.487 | 0.241 |
| ★ v9 | 30 | 0.563 | 0.270 |
| v3_low | 20 | 0.492 | 0.270 |
| v6 | 22 | 0.508 | 0.281 (n=5) |
| v5_low / v3_none / v5_none / v4_none | 20~22 | — | 0.303~0.329 |

## 段（gold 113 块）—— v8 / v9 第一第二

| 方案 | 块数 | F1(0) | WinDiff |
| --- | ---: | ---: | ---: |
| ★ v8 | 107 | 0.616 | **0.222** (n=5, 0.197~0.286) |
| ★ v9 | 94 | 0.624 | 0.226 |
| v5_none | 83 | 0.505 | 0.274 |
| v3_none | 69 | 0.544 | 0.275 |
| v6 | 54 | 0.479 | 0.300 (n=5) |
| ★ v7 | 83 | 0.433 | 0.339 |
| v3_low / v5_low | 42~44 | — | 0.342~0.350 |

## 章（gold 8 块）—— 新版本垫底三名

| 方案 | 块数 | F1(0) | WinDiff |
| --- | ---: | ---: | ---: |
| v5_low | 8 | **1.000** | **0.000** |
| v3_low | 7 | 0.923 | 0.002 |
| v3_none | 6 | 0.667 | 0.008 |
| v5_none | 7 | 0.308 | 0.010 |
| v4_none | 5 | 0.182 | 0.034 |
| v6 | 8 | 0.714 | 0.057 (n=5) |
| ★ v7 | 11 | 0.353 | 0.279 |
| ★ v8 | 12 | 0.737 | 0.284 (n=5) |
| ★ v9 | 14 | 0.600 | 0.351 |

**v5_low 的章边界与 gold_v3 完全一致**：

```
v5_low   [4, 26, 34, 80, 557, 923, 968]
gold_v3  [4, 26, 34, 80, 557, 923, 968]   ← 7/7 精确
gold_v2  [4, 26, 34, 80, 557, 923, 943]   ← 只有末边界不同
v8 首跑  [4, 26, 34, 80, 467, 557, 923, 943, 968]   ← 多切 467 与 943
```

顺带解开一个旧疑问：v6 当初在 968 切章、被 gold_v2 判为「漏掉 943」，
**在 gold_v3 下反而是对的**。两套 gold 在章级只差这一刀，而那一刀正是
约翰福音 9 章那段的归属（gold_v2 提为独立章，gold_v3 留在收尾章内）。

## 结论与下一步

1. **在 gold_v3 口径下，我做的改动在节/段级是有效的**：v8 两级都是第一，
   且 n=5 的区间（节 0.185~0.264、段 0.197~0.286）整体优于 v6（0.189~0.303、0.224~0.342）。
2. **但同一批改动在章级是有害的**：v7/v8/v9 是章级垫底三名。
   v8 的章规则与 v6 字节一致，仍然过切 —— 是段长收紧的连带效应。
3. **最优组合是现成的**：v5_low / v6 的章行为（0.000 / 0.057）+ v8 的段行为（0.222）。
4. **v9 的教训重演**：v9 加了「章可长达数百行」的说明，但同时把段长 20→30，
   两个变量一起动，结果章级反而更差（0.351）。**需要干净的单变量测试**：
   v10 = v8 + 仅加章长度说明，段长维持 20。

---

# v10 的结果：章级修复成立，但代价是段级（2026-09-22）

三个版本均 n=5，基准 gold_v3。

| 层级 | 方案 | 块数（各次） | 中位 | WinDiff 中位 | 区间 | 最差 |
| --- | --- | --- | ---: | ---: | --- | ---: |
| 章 (8) | v6 | 6, 8, 8, 12, 12 | 8 | **0.057** | 0.000~0.338 | 0.338 |
| | **v10** | 8, 9, 9, 9, 10 | 9 | 0.067 | 0.005~0.139 | **0.139** |
| | v8 | 10, 11, 12, 13, 13 | 12 | 0.284 | 0.097~0.341 | 0.341 |
| 节 (43) | v8 | 28, 34, 36, 36, 44 | 36 | **0.220** | 0.185~0.264 | 0.264 |
| | **v10** | 22, 29, 30, 32, 41 | 30 | 0.241 | 0.178~0.306 | 0.306 |
| | v6 | 20, 22, 22, 38, 39 | 22 | 0.281 | 0.189~0.303 | 0.303 |
| 段 (113) | v8 | 86, 100, 107, 109, 111 | 107 | **0.222** | 0.197~0.286 | 0.286 |
| | **v10** | 94, 95, 104, 118, 141 | 104 | 0.261 | 0.232~0.278 | 0.278 |
| | v6 | 49, 52, 54, 96, 110 | 54 | 0.300 | 0.224~0.342 | 0.342 |

## 三级 WindowDiff 中位之和

| 方案 | 合计 |
| --- | ---: |
| **v10** | **0.569** |
| v6 | 0.639 |
| v8 | 0.726 |

**v10 是本次会话第一个综合成绩超过 v6 的版本。**

## 三条结论

1. **章级修复成立。** 单变量改动（只加一句章长度说明）把章级
   从 v8 的 0.284 拉回 0.067，与 v6 的 0.057 基本持平。
   **而且比 v6 更可靠**：v10 最差一次 0.139，v6 最差 0.338
   （v6 有两次跑出 12 章，v10 最多 10 章）。
   要稳定的章级结构，v10 优于 v6。

2. **代价是段级。** v10 段级 0.261，差于 v8 的 0.222（区间 0.232~0.278
   对 0.197~0.286，重叠但中位明确分开）。节级同理（0.241 对 0.220）。
   **我只加了一句「章」的说明，却让段级退了 0.039。**

3. **跨层级串扰是这份提示词的固有特性，已两次观测到：**
   - v8 只收紧段长 → 章级从 v6 的 0.057 崩到 0.284
   - v10 只加章长度说明 → 段级从 v8 的 0.222 退到 0.261

   说明模型是把三级指令当成一个整体的"粒度取向"在读，
   不是按层级独立执行。想单独调某一层而不动其他层，靠措辞做不到。

## 该选哪个版本

取决于下游用途：

- **做 RAG 的 chunk 切分** —— 段级最重要，选 **v8**（段 0.222）。
  章级过切对检索影响小，因为 chunk 是段。
- **要平衡的三级结构**（比如生成目录、做层级导航）—— 选 **v10**（合计 0.569）。
- **只要章级稳定** —— v10 优于 v6（最差 0.139 对 0.338）。

v6 在任何一个单项上都不是最优，只是三项都不太差。


---

# 定案后的结论（以 gold_v3 为唯一基准）

完整数据见 `output/gold_v3_baseline.txt`。WindowDiff 越低越好；n=5 的取中位数。

| 层级 | 最优 | 块数 | F1(0) | WinDiff | n |
| --- | --- | ---: | ---: | ---: | ---: |
| 章 (8) | **v5_low** | 8 | **1.000** | **0.000** | 1 |
| 节 (43) | **v8** | 36 | 0.609 | **0.220** | 5 |
| 段 (113) | **v8** | 107 | 0.616 | **0.222** | 5 |
| 三级合计 | **v10** | — | — | **0.569** | 5 |

## 三条要点

1. **章级 v5_low 完全命中**：边界 `[4, 26, 34, 80, 557, 923, 968]` 与 gold_v3 一字不差。
   但它只跑过 1 次，而 v6 五次的章块数是 `[6, 8, 8, 12, 12]` —— 方差很大，
   v5_low 那次有多少是运气无从判断。要定论得让它也跑 5 次。
2. **节和段都是 v8 第一**，且都是 n=5 的中位，区间也整体优于 v6。
   这两层的结论比章级可靠。
3. **没有任何版本同时拿下章和节**：v5_low 章级满分但节级排第 7（0.303）；
   v8 节段双冠但章级 0.284 垫底。唯一三级都不差的是 v10（合计 0.569）。

## 下一步的两个选项

- **两阶段调用**：章级用 v5_low 定骨架，再把每一章单独喂给 v8 切节和段。
  顺带绕开跨层级串扰（本次调提示词反复碰壁的根源）。
- **补齐 v5_low 的 n=5**，确认它的章级是真本事还是单跑运气。

---

# 多次调用怎么用：投票优于挑最优（2026-09-22，未启用）

问题：同一提示词跑 5 次结果差别很大，生产时没有 gold，挑不了最好的那次。

用已有的 v6 / v8 / v10 各 5 次数据验证「边界投票」——
相距 ≤2 行的边界算同一刀，得票 ≥k 的保留，位置取该簇中位数。
这是确定性规则，不看 gold，生产可直接用。

| 层级 | 方案 | 各次 WinDiff | 中位 | 最优（需 gold，作弊） | 投票≥3 | 投票≥2 |
| --- | --- | --- | ---: | ---: | ---: | ---: |
| 节 | v6 | .189 .221 .281 .283 .303 | 0.281 | 0.189 | 0.245 | **0.193** |
| | v8 | .185 .197 .220 .229 .264 | 0.220 | 0.185 | 0.215 | **0.194** |
| | v10 | .178 .218 .241 .283 .306 | 0.241 | 0.178 | 0.241 | **0.200** |
| 段 | v6 | .224 .273 .300 .328 .342 | 0.300 | 0.224 | 0.274 | **0.238** |
| | v8 | .197 .208 .222 .234 .286 | 0.222 | 0.197 | **0.186** | 0.203 |
| | v10 | .232 .261 .261 .274 .278 | 0.261 | 0.232 | **0.230** | 0.230 |

三条结论：

1. **投票每一格都显著优于中位数**，无一例外。
2. **v8 段级投票≥3 得 0.186，低于它最好的单跑 0.197。**
   投票产出的是一个新结果而非从 5 个里挑一个，所以能超过全部 5 次。
3. **阈值按方向选**：节级 ≥2 更好（节少，怕漏切）；
   段级 v8/v10 ≥3 更好（怕过切），v6 ≥2 更好（它本就切太少）。

用法上应当分开：
- **评测提示词**用中位数，诚实反映「随便跑一次什么水平」；
- **实际交付**用投票结果。

本轮按用户要求未启用投票，记录备查。

---

# 节级隔离评测：章给 gold，只切节，n=5（2026-09-22）

设定：章直接读 `output/gold_v3.md` 的 8 个区间（**只给行号，不给 gold 的章标题**，
否则是泄题），每章一次调用，`--stop-after se`，提示词见
`output/prompts_节切分_纯文本.md`。

| | 节数 | F1(0) | F1(±3) | Pk | WinDiff |
| --- | ---: | ---: | ---: | ---: | ---: |
| 第1轮 | **43** | **0.714** | 0.810 | 0.150 | **0.163** |
| 第2轮 | 47 | 0.636 | 0.773 | 0.190 | 0.206 |
| 第3轮 | 46 | 0.598 | 0.736 | 0.207 | 0.231 |
| 第4轮 | 45 | 0.605 | 0.791 | 0.182 | 0.198 |
| 第5轮 | 45 | 0.558 | 0.698 | 0.258 | 0.276 |
| **中位** | **45** | 0.605 | 0.773 | 0.190 | **0.206** |

## 与一口气出三级的对比

| 方案 | 节数中位 | WinDiff 中位 | 区间 |
| --- | ---: | ---: | --- |
| **本轮（只切节，章给 gold）** | **45** | **0.206** | 0.163~0.276 |
| v8 一口气三级 | 36 | 0.220 | 0.185~0.264 |
| v10 一口气三级 | 30 | 0.241 | 0.178~0.306 |
| v6 一口气三级 | 22 | 0.281 | 0.189~0.303 |

**中位数是目前节级最好的 0.206，但区间与 v8 大幅重叠，按 n=5 的标准领先尚未确立。**

**确立的是稳定性**：节数摆动只有 4（43~47），v8 是 16（28~44）。

前提须挑明：本轮的章是直接给的 gold，v8/v10/v6 需自己找章，**不是同条件对比**。
本轮测的是节提示词单独的能力。

## 逐章节数：8 章里 5 章五轮完全一致

| 章 | 行数 | 五轮 | gold | |
| ---: | ---: | --- | ---: | --- |
| 1 | 3 | 1,1,1,1,1 | 1 | 全对 |
| 2 | 22 | 3,3,3,3,3 | 2 | 稳定多 1，见下 |
| 3 | 8 | 1,1,1,1,1 | 1 | 全对 |
| 4 | 46 | 4,4,4,4,4 | 4 | 全对 |
| 5 | 477 | 17,19,18,18,19 | 18 | 中位 18 = gold |
| 6 | 366 | 14,16,15,15,14 | 14 | 中位 15 |
| 7 | 45 | 2,2,3,2,2 | 2 | 中位 2 = gold |
| 8 | 2 | 1,1,1,1,1 | 1 | 全对 |

节长中位 20，与 gold 的 20 相同（max 52，gold 42）。

## 第2章的稳定分歧不是模型的错

gold 把整段诵读当 1 节 `[9-25]`；模型五轮都切成 `[9-18]`（约伯记1:1-5，地上的约伯）
与 `[19-25]`（1:6-8，天上的众子与撒但）。

按节的判据「主语或问题不同，就是两节」，模型是对的——主语从约伯换成了神的众子与撒但，
而且 1-5 / 6-8 这个分界正是讲者在行 34 自己宣告的两部分结构。
gold 选择整段不切，是把「诵读经文」当成一个完整的言语行为。**两种都讲得通，是口径分歧。**
